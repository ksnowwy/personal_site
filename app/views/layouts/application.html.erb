<!DOCTYPE html>
<html>
  <head>
	<meta charset="UTF-8">
    <title><%= title %></title>
    <%= csrf_meta_tag %>
    <%= render 'layouts/stylesheets' %>
	<%= javascript_include_tag :defaults %>
    <%= javascript_include_tag 'jquery-1.6.2.min', 'jquery-ui-1.8.16.custom.min', 'kstwitter', 'nested_form', 'jquery-ui-slider' %>
  </head>
  <body class="jsoff">
    <div id="wrapper">
      <%= render 'layouts/header' %>
        <% flash.each do |key, value| %>
          <%= content_tag(:div, value, :class => "flash #{key}") %>
        <% end %> 
      <%= yield %>
    </div><!--wrapper-->
      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
  </body>
</html>