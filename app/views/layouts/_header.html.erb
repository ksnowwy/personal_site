<header class="site_header">
  <h1><%= link_to image_tag("bluelogo80.png"), root_path, :title => "Kelly Smith-Holbourn" %></h1>
  <nav>
    <ul>
      <li><%= link_to "Home", root_path, :class => "home-link" %></li>
      <li><%= link_to "Projects", projects_path %></li>
      <li><%= link_to "About", about_path %></li>
    </ul>
  </nav>
  <% if signed_in? %>
    <p class="welcome_user">Welcome, <%= link_to current_user.name, current_user %></p>
  <% end %>
  <% if signed_in? && current_user.admin? %>
    <p class="new_article_link"><%= link_to "Write an article", "/articles/new" %></p>
  <% end %>
</header>
<div class="main_content">